# Shuu Corporation React Site

React Routerで `/`, `/service`, `/about`, `/contact` に分割したVite + React + TypeScript構成です。

## 開発

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```

## Vercel

GitHubにpushしてVercelでImportしてください。SPAルーティング用に `vercel.json` を入れています。
